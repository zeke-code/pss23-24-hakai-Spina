package com.zekecode.hakai.components.ball;

import com.zekecode.hakai.core.Component;

/** A marker component to identify the ball entity. */
public class BallComponent implements Component {
  // No data needed. Its presence on an entity is what matters.
}
